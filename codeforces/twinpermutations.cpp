#include <bits/stdc++.h>

using namespace std;

void solve() {
	int n;
	cin >> n;
	vector<int> v(n);
	vector<int> p(n);

	for(int i = 0;i < n;i++) {
		cin >> v[i];
		p[i] = v[i];
	}

}

int32_t main() {
	int t;
	cin >> t;

	while(t--) {
		solve();
	}
	return 0;
}
