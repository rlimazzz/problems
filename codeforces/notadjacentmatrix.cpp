#include <bits/stdc++.h>

using namespace std;

int32_t main() {
    int t;
    cin >> t;

    while(t--) {
        int n;
        cin >> n;
        
        vector<vector<int>> v(n, (n, 0));

        set


        for(int i = 0;i < n;i++) {
            v[i][i] = i + 1;
        }

        for(int i = 0;i < n;i++) {
            for(int j = 0;j < n;j++) {

            }
        }

        
    }
    return 0;
}
